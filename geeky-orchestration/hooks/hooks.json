{
  "description": "Geeky Orchestration plugin hooks — warn before edits to frozen planning artifacts. Uses the Python hook by default for cross-platform reliability. To prefer the PowerShell version on Windows, change the command below to: pwsh -NoProfile -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/guard-planning-contract.ps1\"",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/guard-planning-contract.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
